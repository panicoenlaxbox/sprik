# Development only — never commit .env.local with real values
# In production, keys are stored encrypted via Electron safeStorage (set through the Settings UI)
OPENAI_API_KEY=
GROQ_API_KEY=
ANTHROPIC_API_KEY=
